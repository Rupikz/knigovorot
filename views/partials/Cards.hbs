<div class="row">
  {{#if this}}
    {{#each this}}
      {{#with this}}
        <div class="col l3 m4 s6">
            <div class="card">
              <div class="card-image waves-effect waves-block waves-light">
                <a href="/id{{ id }}">
                  <img class="activator" src="images/books/{{ id_image_book.[0] }}">
                </a>
                <i id="icon-{{ id }}" class="material-icons icon-favorite" onclick="like({{ id }})" value="{{ id }}">favorite_border</i>
              </div>
              <a href="/id{{ id }}">
                <div class="card-content">
                  <span class="card-title activator grey-text text-darken-4">
                    {{ name_book }}
                  </span>
                  <span class="card-writer activator grey-text text-darken-2">
                    {{ author_book }}
                  </span>
                  <span class="activator grey-text text-darken-1">
                      <span class="сard-price">{{ price }} руб</span>
                    </span>
                </div>
            </div>
          </a>
        </div>
      {{/with}}
    {{/each}}
  {{else}}
    <h4 class="default">Книг нет</h4>
  {{/if}}
</div>
